//# 상수 및 변수(let, var)
let one: Int  // 상수: 변하지 않는 수
var two: Int  // 변수: 변하는 수

var red, green, blue: String  // 묶음 정의
var number = 123  // 유형(type)은 생략가능



//* 변수명 지정
// 예약어, 공백, 숫자, 연산자 기호를 제외한 거의 모든 유니코드를 쓸 수 있다.
let 이름 = "홍길동"
let 🐕🦅 = "개새"
